__pycache__/
*.pyc
.venv/

# 모델/엔진 파일은 용량이 커서 git에 올리지 않음 (원격 서버에서 빌드)
models/*.onnx
models/*.engine

# proto/control_api.proto에서 재생성 가능한 산출물 — 원격 서버에서 protoc으로 생성
src/pb/*_pb2.py
src/pb/*_pb2_grpc.py

# OMC 툴 상태
.omc/
